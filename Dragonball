ocal Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local aimbotEnabled = false
local espEnabled = false
local espObjects = {}
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "PremiumMenu"
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
MainFrame.Position = UDim2.new(0.05, 0, 0.2, 0)
MainFrame.Size = UDim2.new(0, 220, 0, 190)
MainFrame.Active = true
MainFrame.Draggable = true
local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame
local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(50, 50, 60)
MainStroke.Thickness = 1.5
MainStroke.Parent = MainFrame
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Parent = MainFrame
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 0, 0, 5)
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Font = Enum.Font.GothamBold
Title.Text = "CHEF MENU"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
local function createStyledButton(name, text, positionY)
local button = Instance.new("TextButton")
button.Name = name
button.Parent = MainFrame
button.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
button.Position = UDim2.new(0.07, 0, 0, positionY)
button.Size = UDim2.new(0.86, 0, 0, 45)
button.Font = Enum.Font.GothamSemibold
button.Text = text .. ": OFF"
button.TextColor3 = Color3.fromRGB(200, 200, 200)
button.TextSize = 14
button.AutoButtonColor = false
local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 8)
corner.Parent = button
local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(255, 75, 75)
stroke.Thickness = 1
stroke.Parent = button
return button
end
local AimButton = createStyledButton("AimButton", "Aimbot", 45)
local EspButton = createStyledButton("EspButton", "ESP", 10
